# Progress ledger — gamestate-single-owner-snapshot

Plan: docs/superpowers/plans/2026-07-12-gamestate-single-owner-snapshot.md
Worktree: .claude/worktrees/gamestate-single-owner-snapshot (branch worktree-gamestate-single-owner-snapshot)
Baseline: cargo build OK; cargo test 526 passed, 0 failed, 17 ignored (base commit 7de68b9)

## Final whole-branch review

Reviewed on opus, full 9-commit range 7de68b9..6da3962. Verdict: **Approved for merge (with minor notes)**. Cross-task seam audit (the key value of a whole-branch pass) walked every deleted app_tx echo/synthetic-packet arm and confirmed a live network-thread writer exists for every field in final code — no orphaned state found (trainer_open initially looked suspect, confirmed covered by `gs.trainer_open.take()`). All Global Constraints hold: arc-swap added exactly once (Task 1), apply_packet function body byte-identical across the whole range (Task 4's packet_handler.rs edit is a mechanical Door-field-removal consequence, not a mutation-semantics change — judged a legitimate exception), Navigator::sync_* + HTTP Arc<Mutex<T>> slots untouched, no bisect hazard from Task 6's tests (pure GameState logic, no dependency on Task 8). Architecture confirmed single-pattern: one publish_snapshot call site, one testzone seed (mutually exclusive with the network thread), two render-side refresh points (poll_external + render_frame), zero remaining old-field reads. Minor (non-blocking): ledger's Task 3 zone_changed audit never explicitly documented checking "book-text presentation" as the plan's Global Constraints line named — on inspection last_book_text turned out to not be an edge-triggered field at all (wholesale overwrite + clone-read, no clear), so no fix needed, just a disclosure gap. Minor (non-blocking, already disclosed in Task 8): OP_UI_LOCAL_ECHO/OP_UI_LOOT_STATE/OP_UI_CLEAR_INVITE match arms in packet_handler.rs are now unreachable dead code (deliberately retained per the plan rather than touching the constrained file further) — optional future cleanup.

## Tasks
- [x] Task 1: complete (commits 7de68b9..a229a64, review clean — minor: report cited game_state.rs:308 for the Clone derive vs actual 309, immaterial. NOTE: implementer subagent initially committed d7c57a6 directly to the shared main checkout by mistake; recovered via cherry-pick onto this branch as a229a64 and `git reset --hard origin/main` on the main checkout, both done with explicit user confirmation.)
- [x] Task 2: complete (commits a229a64..96e1542, review clean/Approved — Important (tracked, not blocking): brief's Step 8 live-client + profiler glance (confirm publish_snapshot's ~100Hz GameState deep-clone isn't costing meaningfully in the 10ms tick budget) was not performed, since this task changes no observable behavior. DEFERRED to Task 3's manual verification, the next task with an actual live-client touchpoint — check tick cadence/CPU there before marking Task 3 done. NOTE: implementer subagent's edits landed correctly but it was interrupted before self-verifying/committing/reporting; controller (this session) independently re-verified cargo build/test and committed.)
- [x] Task 3: complete (commits 96e1542..feea31d, review clean/Approved, 0 findings. Step 0 zone_changed audit confirmed safe: App's zone-reload trigger already compares self.scene.zone vs self.current_zone, never reads zone_changed; scene build stays on self.game_state until Task 4 per plan. Deferred Task 2 profiler glance on publish_snapshot's per-tick cost: not independently measured — live run showed steady 377-384fps with no stutter (render thread signal, not network thread's ~100Hz clone cost); still formally unverified, not blocking. Manual live-client check performed against real EQEmu server: zone render, HUD, target round-trip via HTTP observe API all confirmed; F10 overlay keypress + mouse-click picking could not be interactively verified (no Wayland input injection in sandbox) but underlying data paths were confirmed instead — disclosed, not a gap.)
- [x] Task 4: complete (commits feea31d..4d44d78, review clean/Approved, 0 findings. Brief said 10 scene.rs test call sites; actual was 14 (4 extra `&sample_state())` sites) — verified as a necessary signature-change consequence, not a shortcut. Unplanned fix disclosed + verified: switching scene-build to game_state_view broke --testzone mode (no network thread ever publishes into the snapshot there) — fixed by seeding game_state_snapshot once inside App::new's existing testzone_mode block; confirmed no-op on the live-server path. Door-click animation behavior (eases ~0.5s, spawn-open renders open immediately) unit-tested + code-inspected but NOT interactively verified against a live server — sandbox has no route to jimbo.lan and testzone has no doors. Still an open verification gap, watch for it if a live-client session becomes available before final review.)
- [x] Task 5: complete (build clean, 530 lib tests pass. Investigated the brief's cadence assumption: confirmed by code trace, not just reading — the network thread's `stream_position` (eq_net/navigation.rs) mirrors the render controller's own `view.pos` into `gs.player_x/y/z` unconditionally every network tick (independent of the 280ms actual OP_ClientUpdate send-to-server gate), and `view.pos` is itself published by the render thread every frame. So `game_state_view.player_x/y/z` tracks the local controller with ~1 tick of latency during WASD — smooth enough for the `nav_dist > 0.01` gate. No velocity-gated alternative needed; brief's Step 2 applied verbatim. Diff matches the brief's 5 steps exactly, no extra edits (see task-5-report.md for a discarded testzone-specific fix I tried, then reverted after live-testing `--testzone` mode via `/v1/move/manual` showed it never builds collision, so `controller.step` never runs there and player position is static regardless — a pre-existing testzone limitation, not something Task 5 touches). Could not exercise the live-server WASD/goto/GM-teleport acceptance test end-to-end — sandbox still has no route to jimbo.lan and local EQEmu containers are stopped; confidence rests on the code-path trace above plus the empirical testzone probe.)
- [x] Task 6: complete (build clean, 530 lib tests pass. Pre-check found a real gap: `gs.target_hp_pct` had NO live network-side writer on the actual published GameState — `apply_set_target` (packet_handler.rs) only ever ran against App's now-vestigial `self.game_state` via the synthetic app_tx mirror, never against the network thread's own `gs` that Navigator::tick mutates and publish_snapshot republishes; and `apply_hp_update`/`apply_mob_health` only ever touched `entities[id].hp_pct`, never `gs.target_hp_pct`. Deleting the App echo without a fix would have frozen the HUD's target HP gauge at `-?-` forever. Fixed beyond the brief's app.rs-only file list: (1) navigation.rs's shared target-select block now seeds target_name/target_hp_pct correctly for BOTH entity and self-target (F1) cases — found a second gap here too, since the player is never present in `entities` (register_spawn special-cases the self-spawn), so rerouting F1 through the shared acts.target path per the brief's literal Step 2 would otherwise have left self-target name/HP stale; (2) game_state.rs's update_hp/update_hp_pct now keep target_hp_pct live-synced whenever an HP update lands for the current target, not just seeded once at selection. target_con needed no fix — apply_consider already sets it correctly on the real published gs via the OP_CONSIDER reply. No live-server verification possible (no route to jimbo.lan, local EQEmu containers stopped) — reasoned from a full grep-every-writer code trace instead; see task-6-report.md for the complete pre-check evidence. Review round 1 flagged Important: no regression test for the two fixes — fixed in commit 4154a5c, 5 new unit tests in game_state.rs (530→535 lib tests), re-review Approved with 0 findings. Full range: 51ab166..4154a5c.)
- [x] Task 7: complete (commit 4154a5c..af1dd44, review clean/Approved, 0 findings. Trivial 3-line deletion — confirmed Navigator::tick (network thread) already independently logs "Clicked door {id}" when it drains the same door_click slot, so this is genuine dedup not a silent logging loss. No live-server verification possible, disclosed honestly.)
- [x] Task 8: complete (build clean, 535 lib tests pass. Step 9b inventory of all 19 `app_tx.send(...)` sites across gameplay.rs/login.rs/navigation.rs found 18 pure mirrors of an adjacent direct `gs` mutation plus 1 confirmed-dead forward (reconnect_via_world's world_rx forward — no opcode exchanged during world-reconnect has an apply_packet handler at all, verified by reading the full match statement); zero sites required porting a new mutation. Deliberately did NOT port apply_ui_loot_state's compensating `pending_loot.clear()` into the loot-session-end site — that was a workaround for the OLD render-side copy that never drained pending_loot itself; the single gs already drains it via pop_front(), so porting the clear would regress multi-corpse loot queues. Minor scope-adjacent simplification in the merchant-buy site: collapsed the now-empty-bodied `if gs.spend_coin(p) { <only the send> }` down to an unconditional `gs.spend_coin(p);`, verified behaviorally identical since spend_coin() itself already no-ops without mutating gs.coin on insufficient funds. `--testzone` mode boot-verified live (15s run, debug zone + 16 billboard models loaded, steady frame cadence, no panics) — this exercises Step 1's snapshot-seeding fix. Live-server flows (login, walking, targeting, doors, loot/invite/item-move/chat-echo/merchant-coin, /debug connection-health transition) NOT verified — sandbox still has no route to jimbo.lan and local EQEmu containers are stopped. See task-8-report.md for the full per-site disposition table. REVIEW: Approved, 0 findings (spec ✅). Reviewer independently re-derived the Step 9b inventory from the af1dd44 source (not trusting the report) — grep counts matched 19/19, dead-forward claim confirmed by reading apply_packet's full match arm list, spot-checked mirror sites all had an adjacent surviving gs mutation. Both disclosed deviations (spend_coin if-collapse, non-ported pending_loot.clear()) independently verified safe. packet_handler.rs confirmed zero-diff. Full range: af1dd44..6da3962.)
